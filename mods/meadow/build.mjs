/** Optional resource regenerate — not required for release builds.
 *  Historical generator expected pack-local .tmp sources; committed src/main/resources is authoritative.
 */
console.log("generateResources: no-op in monorepo release layout (resources are committed).");
